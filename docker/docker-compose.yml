services:
  mortgage-rates-mvp:
    build:
      context: ..
      dockerfile: docker/Dockerfile
    image: mortgage-rates-mvp:dev
    ports:
      - "8081:8080"
    environment:
      - SPRING_PROFILES_ACTIVE=default
